public class tree {

    public Node search (Node T, Integer i){
	int c = t.getData().compareTo(i);
	while (T!=null){
	    if (c>0){
		T = T.getRight();
	    } else if (c<0){
		T = T.getLeft();
	    } else {
		return T;
	    }
	}
    }
}
